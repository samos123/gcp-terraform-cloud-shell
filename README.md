# Google Cloud Shell image with TerraForm


Go to Cloud Shell environment settings and specify the following image as your custom image:
`docker pull gcr.io/sam-playground-123/sam-cloud-shell:latest`.
